# Claude Code 指南

## 语言

除非用户切换为英文，否则始终用中文回复。

## Electron 开发

- 每次修改代码后，必须自动执行 `npm run build` 重新构建，不要让用户手动执行
- 本项目使用 Vite + Electron。开发时确保 dev server 已启动，Electron 从 `localhost` 加载，而非 `dist/`
- 启动前检查端口冲突
- 不要设置会覆盖已加载页面的硬超时

## 项目

**花木菜菜子的创作者助手**：帮助内容创作者管理和生产创作内容的工具集

## 项目结构

- **src/** - 源代码
- **docs/** - 文档
- **notes/** - 笔记和草稿
- **dist/** - 构建输出

## 规则

- 文档使用中文，文件名使用英文
- 不自动提交，除非用户明确要求
- 敏感信息存 `.env.local`，不入 Git
- 这是一个写作/创作类项目，优先考虑内容质量和创作流程
