# Phase 1: 知识库 RAG 检索系统 实施计划

> **对于 Claude：** 必需的子技能：使用 superpowers:executing-plans 按任务逐步实施此计划。

**目标：** 将知识库的"全量拼接"替换为真正的 RAG（Retrieval-Augmented Generation），通过 Embedding + 向量检索 + Re-rank，将每次 prompt 从几十万 token 降到几千 token，质量不降反升。

**架构：** 文件上传时自动分块（md 按 heading，pdf/txt 按段落，300-500 字 + 50 字 overlap）→ 调用 Gemini text-embedding-004 生成向量 → 存入 IndexedDB `kbChunks` store + JS 内存缓存。查询时对用户消息做 embedding → 余弦相似度 top-K=6 → Gemini Flash re-rank 压到 3 条 → 注入 prompt。

**技术栈：** Gemini text-embedding-004（embedding）、Gemini Flash（re-rank）、IndexedDB（持久化）、JS 内存 Float32Array（向量检索）、Electron IPC（API 调用）

---

## 任务 1: 新增 Embedding IPC 通道

**文件：**
- 修改：`electron/main.js` — 新增 `gemini:embed` IPC handler
- 修改：`electron/preload.js` — 暴露 `geminiEmbed` 方法

**步骤 1: 在 main.js 添加 embedding IPC handler**

在 `ipcMain.handle('gemini:chat', ...)` 之后添加：

```javascript
ipcMain.handle('gemini:embed', async (event, { apiKey, texts }) => {
  const { fetch: undiciFetch, ProxyAgent } = require('undici')

  let dispatcher
  try {
    const proxyInfo = require('child_process').execSync('scutil --proxy', { encoding: 'utf8' })
    const portMatch = proxyInfo.match(/HTTPSPort\s*:\s*(\d+)/)
    const enableMatch = proxyInfo.match(/HTTPSEnable\s*:\s*(\d+)/)
    if (portMatch && enableMatch && enableMatch[1] === '1') {
      dispatcher = new ProxyAgent(`http://127.0.0.1:${portMatch[1]}`)
    }
  } catch (e) { /* 无代理 */ }

  const url = `https://generativelanguage.googleapis.com/v1beta/models/text-embedding-004:batchEmbedContents?key=${apiKey}`
  const body = JSON.stringify({
    requests: texts.map(text => ({
      model: 'models/text-embedding-004',
      content: { parts: [{ text }] }
    }))
  })

  const fetchOptions = {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body
  }
  if (dispatcher) fetchOptions.dispatcher = dispatcher

  const response = await undiciFetch(url, fetchOptions)
  const json = await response.json()

  if (!response.ok || json.error) {
    const msg = json.error ? `Embedding错误 ${json.error.code}: ${json.error.message}` : `HTTP ${response.status}`
    throw new Error(msg)
  }

  return json.embeddings.map(e => e.values)
})
```

**步骤 2: 在 preload.js 暴露 geminiEmbed**

```javascript
geminiEmbed: (params) => ipcRenderer.invoke('gemini:embed', params),
```

**步骤 3: 在 main.js 添加 re-rank IPC handler**

在 `gemini:embed` handler 之后添加：

```javascript
ipcMain.handle('gemini:rerank', async (event, { apiKey, query, candidates }) => {
  const { fetch: undiciFetch, ProxyAgent } = require('undici')

  let dispatcher
  try {
    const proxyInfo = require('child_process').execSync('scutil --proxy', { encoding: 'utf8' })
    const portMatch = proxyInfo.match(/HTTPSPort\s*:\s*(\d+)/)
    const enableMatch = proxyInfo.match(/HTTPSEnable\s*:\s*(\d+)/)
    if (portMatch && enableMatch && enableMatch[1] === '1') {
      dispatcher = new ProxyAgent(`http://127.0.0.1:${portMatch[1]}`)
    }
  } catch (e) { /* 无代理 */ }

  const prompt = `你是一个相关性评分器。给定一个查询和若干候选文本片段，为每个片段打分（0-10），只返回 JSON 数组。

查询：${query}

候选片段：
${candidates.map((c, i) => `[${i}] ${c}`).join('\n\n')}

返回格式：[{"index": 0, "score": 8}, ...]，只返回 JSON，不要其他文字。`

  const url = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`
  const body = JSON.stringify({
    contents: [{ role: 'user', parts: [{ text: prompt }] }]
  })

  const fetchOptions = {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body
  }
  if (dispatcher) fetchOptions.dispatcher = dispatcher

  const response = await undiciFetch(url, fetchOptions)
  const json = await response.json()

  if (!response.ok || json.error) {
    const msg = json.error ? `Rerank错误 ${json.error.code}: ${json.error.message}` : `HTTP ${response.status}`
    throw new Error(msg)
  }

  const text = json.candidates[0].content.parts[0].text
  const jsonMatch = text.match(/\[[\s\S]*\]/)
  return jsonMatch ? JSON.parse(jsonMatch[0]) : []
})
```

**步骤 4: 在 preload.js 暴露 geminiRerank**

```javascript
geminiRerank: (params) => ipcRenderer.invoke('gemini:rerank', params),
```

**步骤 5: 验证构建**

运行：`npm run build`
预期：构建成功，无错误

**步骤 6: 提交**

```bash
git add electron/main.js electron/preload.js
git commit -m "feat: add gemini embedding and rerank IPC channels"
```

---

## 任务 2: 文本分块服务 (chunker)

**文件：**
- 创建：`src/services/chunker.js`

**步骤 1: 创建 chunker.js**

```javascript
const TARGET_SIZE = 400   // 目标 chunk 大小（字符）
const MIN_SIZE = 200
const MAX_SIZE = 600
const OVERLAP = 50

export function chunkMarkdown(text, fileName) {
  const sections = splitByHeadings(text)
  const chunks = []
  for (const section of sections) {
    if (section.content.length <= MAX_SIZE) {
      chunks.push({ text: section.content, heading: section.heading, fileName })
    } else {
      const sub = splitBySize(section.content, TARGET_SIZE, OVERLAP)
      sub.forEach(t => chunks.push({ text: t, heading: section.heading, fileName }))
    }
  }
  return chunks
}

export function chunkPlainText(text, fileName) {
  const paragraphs = text.split(/\n\s*\n/).filter(p => p.trim())
  const chunks = []
  let buffer = ''

  for (const para of paragraphs) {
    if (buffer.length + para.length > MAX_SIZE && buffer.length >= MIN_SIZE) {
      chunks.push({ text: buffer.trim(), heading: null, fileName })
      const overlapText = buffer.slice(-OVERLAP)
      buffer = overlapText + para
    } else {
      buffer += (buffer ? '\n\n' : '') + para
    }
  }
  if (buffer.trim()) {
    chunks.push({ text: buffer.trim(), heading: null, fileName })
  }
  return chunks
}

export function chunkDocument(text, fileName) {
  const ext = fileName.toLowerCase()
  if (ext.endsWith('.md') || ext.endsWith('.markdown')) {
    return chunkMarkdown(text, fileName)
  }
  return chunkPlainText(text, fileName)
}

function splitByHeadings(text) {
  const lines = text.split('\n')
  const sections = []
  let current = { heading: null, content: '' }

  for (const line of lines) {
    const headingMatch = line.match(/^(#{1,6})\s+(.+)/)
    if (headingMatch) {
      if (current.content.trim()) sections.push(current)
      current = { heading: headingMatch[2].trim(), content: line + '\n' }
    } else {
      current.content += line + '\n'
    }
  }
  if (current.content.trim()) sections.push(current)
  return sections
}

function splitBySize(text, targetSize, overlap) {
  const result = []
  let start = 0
  while (start < text.length) {
    let end = Math.min(start + targetSize + 100, text.length)
    // 尝试在句号、换行处断开
    if (end < text.length) {
      const slice = text.slice(start, end)
      const breakPoint = Math.max(
        slice.lastIndexOf('。'),
        slice.lastIndexOf('\n'),
        slice.lastIndexOf('. '),
        slice.lastIndexOf('！'),
        slice.lastIndexOf('？')
      )
      if (breakPoint > targetSize * 0.5) {
        end = start + breakPoint + 1
      }
    }
    result.push(text.slice(start, end))
    start = end - overlap
  }
  return result
}
```

**步骤 2: 验证构建**

运行：`npm run build`
预期：构建成功

**步骤 3: 提交**

```bash
git add src/services/chunker.js
git commit -m "feat: add text chunking service for RAG"
```

---

## 任务 3: Embedding 服务 + 向量缓存

**文件：**
- 创建：`src/services/embeddingService.js`

**步骤 1: 创建 embeddingService.js**

```javascript
const GEMINI_API_KEY = import.meta.env.VITE_GEMINI_API_KEY || ''
const BATCH_SIZE = 20  // Gemini batchEmbedContents 每批最多 100，保守用 20

// 内存向量缓存：fileId → { chunks: [...], vectors: Float32Array[] }
const vectorCache = new Map()

export async function embedTexts(texts) {
  if (!GEMINI_API_KEY) throw new Error('请设置 Gemini API 密钥')
  const results = []
  for (let i = 0; i < texts.length; i += BATCH_SIZE) {
    const batch = texts.slice(i, i + BATCH_SIZE)
    const vectors = await window.electronAPI.geminiEmbed({
      apiKey: GEMINI_API_KEY,
      texts: batch
    })
    results.push(...vectors)
  }
  return results
}

export function cosineSimilarity(a, b) {
  let dot = 0, normA = 0, normB = 0
  for (let i = 0; i < a.length; i++) {
    dot += a[i] * b[i]
    normA += a[i] * a[i]
    normB += b[i] * b[i]
  }
  return dot / (Math.sqrt(normA) * Math.sqrt(normB))
}

export async function searchSimilar(queryVector, topK = 6) {
  const results = []
  for (const [fileId, entry] of vectorCache) {
    for (let i = 0; i < entry.vectors.length; i++) {
      const score = cosineSimilarity(queryVector, entry.vectors[i])
      results.push({ ...entry.chunks[i], fileId, score })
    }
  }
  results.sort((a, b) => b.score - a.score)
  return results.slice(0, topK)
}

export async function rerankChunks(query, chunks, topN = 3) {
  if (!GEMINI_API_KEY) return chunks.slice(0, topN)
  try {
    const candidates = chunks.map(c => c.text)
    const scores = await window.electronAPI.geminiRerank({
      apiKey: GEMINI_API_KEY,
      query,
      candidates
    })
    // 合并分数并排序
    const scored = chunks.map((chunk, i) => {
      const found = scores.find(s => s.index === i)
      return { ...chunk, rerankScore: found ? found.score : 0 }
    })
    scored.sort((a, b) => b.rerankScore - a.rerankScore)
    return scored.slice(0, topN)
  } catch (e) {
    console.warn('Re-rank 失败，回退到向量排序:', e)
    return chunks.slice(0, topN)
  }
}

export function cacheVectors(fileId, chunks, vectors) {
  vectorCache.set(fileId, { chunks, vectors })
}

export function removeCachedVectors(fileId) {
  vectorCache.delete(fileId)
}

export function clearVectorCache() {
  vectorCache.clear()
}

export function getVectorCache() {
  return vectorCache
}
```

**步骤 2: 验证构建**

运行：`npm run build`
预期：构建成功

**步骤 3: 提交**

```bash
git add src/services/embeddingService.js
git commit -m "feat: add embedding service with vector cache and rerank"
```

---

## 任务 4: IndexedDB kbChunks store + 持久化

**文件：**
- 修改：`src/services/storageService.js` — 升级 DB_VERSION 到 3，新增 `kbChunks` store，新增 CRUD 函数

**步骤 1: 升级数据库版本，新增 kbChunks store**

在 `storageService.js` 中：

1. 将 `DB_VERSION` 从 `2` 改为 `3`
2. 在 `STORE_NAMES` 中新增 `KB_CHUNKS: 'kbChunks'`
3. 在 `initDB()` 的 `onupgradeneeded` 中添加 v3 升级逻辑：

```javascript
// v3: 创建 kbChunks 存储空间（RAG 向量检索）
if (event.oldVersion < 3) {
  const chunkStore = db.createObjectStore(STORE_NAMES.KB_CHUNKS, { keyPath: 'id', autoIncrement: true })
  chunkStore.createIndex('byFileId', 'fileId', { unique: false })
  chunkStore.createIndex('byKbId', 'kbId', { unique: false })
}
```

**步骤 2: 新增 chunk CRUD 函数**

在 `storageService.js` 末尾添加：

```javascript
export async function saveChunks(fileId, kbId, chunks, vectors) {
  const db = await getDB()
  const tx = db.transaction([STORE_NAMES.KB_CHUNKS], 'readwrite')
  const store = tx.objectStore(STORE_NAMES.KB_CHUNKS)
  const ids = []
  for (let i = 0; i < chunks.length; i++) {
    const record = {
      fileId,
      kbId,
      text: chunks[i].text,
      heading: chunks[i].heading || null,
      fileName: chunks[i].fileName,
      vector: Array.from(vectors[i]),
      createdAt: new Date().toISOString()
    }
    const req = store.add(record)
    ids.push(new Promise((resolve, reject) => {
      req.onsuccess = () => resolve(req.result)
      req.onerror = () => reject(req.error)
    }))
  }
  return Promise.all(ids)
}

export async function getChunksByFile(fileId) {
  return new Promise(async (resolve, reject) => {
    try {
      const db = await getDB()
      const tx = db.transaction([STORE_NAMES.KB_CHUNKS], 'readonly')
      const store = tx.objectStore(STORE_NAMES.KB_CHUNKS)
      const index = store.index('byFileId')
      const req = index.getAll(fileId)
      req.onsuccess = () => resolve(req.result || [])
      req.onerror = () => reject(req.error)
    } catch (e) { reject(e) }
  })
}

export async function getChunksByKb(kbId) {
  return new Promise(async (resolve, reject) => {
    try {
      const db = await getDB()
      const tx = db.transaction([STORE_NAMES.KB_CHUNKS], 'readonly')
      const store = tx.objectStore(STORE_NAMES.KB_CHUNKS)
      const index = store.index('byKbId')
      const req = index.getAll(kbId)
      req.onsuccess = () => resolve(req.result || [])
      req.onerror = () => reject(req.error)
    } catch (e) { reject(e) }
  })
}

export async function deleteChunksByFile(fileId) {
  return new Promise(async (resolve, reject) => {
    try {
      const db = await getDB()
      const tx = db.transaction([STORE_NAMES.KB_CHUNKS], 'readwrite')
      const store = tx.objectStore(STORE_NAMES.KB_CHUNKS)
      const index = store.index('byFileId')
      const req = index.getAll(fileId)
      req.onsuccess = () => {
        const records = req.result || []
        records.forEach(r => store.delete(r.id))
        resolve(records.length)
      }
      req.onerror = () => reject(req.error)
    } catch (e) { reject(e) }
  })
}

export async function deleteChunksByKb(kbId) {
  return new Promise(async (resolve, reject) => {
    try {
      const db = await getDB()
      const tx = db.transaction([STORE_NAMES.KB_CHUNKS], 'readwrite')
      const store = tx.objectStore(STORE_NAMES.KB_CHUNKS)
      const index = store.index('byKbId')
      const req = index.getAll(kbId)
      req.onsuccess = () => {
        const records = req.result || []
        records.forEach(r => store.delete(r.id))
        resolve(records.length)
      }
      req.onerror = () => reject(req.error)
    } catch (e) { reject(e) }
  })
}
```

**步骤 3: 验证构建**

运行：`npm run build`
预期：构建成功

**步骤 4: 提交**

```bash
git add src/services/storageService.js
git commit -m "feat: add kbChunks IndexedDB store for RAG vectors"
```

---

## 任务 5: 文件上传时自动分块 + Embedding

**文件：**
- 修改：`src/services/storageService.js` — `uploadKBFile()` 函数末尾触发分块+embedding
- 创建：`src/services/ragPipeline.js` — 编排 chunk → embed → 存储 的完整流程

**步骤 1: 创建 ragPipeline.js**

```javascript
import { chunkDocument } from './chunker'
import { embedTexts, cacheVectors } from './embeddingService'
import { saveChunks, getChunksByKb } from './storageService'

export async function indexFile(fileId, kbId, fileName, textContent) {
  const chunks = chunkDocument(textContent, fileName)
  if (chunks.length === 0) return

  const texts = chunks.map(c => c.text)
  const vectors = await embedTexts(texts)

  await saveChunks(fileId, kbId, chunks, vectors)
  cacheVectors(fileId, chunks, vectors)

  return { chunksCount: chunks.length }
}

export async function loadKbVectorsToCache(kbId) {
  const { cacheVectors: cache } = await import('./embeddingService')
  const records = await getChunksByKb(kbId)

  const byFile = new Map()
  for (const r of records) {
    if (!byFile.has(r.fileId)) byFile.set(r.fileId, { chunks: [], vectors: [] })
    const entry = byFile.get(r.fileId)
    entry.chunks.push({ text: r.text, heading: r.heading, fileName: r.fileName })
    entry.vectors.push(r.vector)
  }

  for (const [fileId, entry] of byFile) {
    cache(fileId, entry.chunks, entry.vectors)
  }

  return records.length
}
```

**步骤 2: 修改 uploadKBFile 触发索引**

在 `storageService.js` 的 `uploadKBFile()` 函数中，`req.onsuccess` 回调里，在 `resolve` 之前异步触发索引（不阻塞上传）：

```javascript
req.onsuccess = () => {
  const savedFile = { ...fileData, id: req.result }
  // 异步触发 RAG 索引（不阻塞上传完成）
  import('./ragPipeline').then(({ indexFile }) => {
    indexFile(req.result, kbId, file.name, fileData.textContent).catch(e =>
      console.warn('RAG 索引失败:', e)
    )
  })
  resolve(savedFile)
}
```

**步骤 3: 修改 deleteKBFile 同步清理 chunks**

在 `storageService.js` 的 `deleteKBFile()` 中，删除文件后同步清理对应 chunks：

```javascript
export async function deleteKBFile(fileId) {
  // 先删 chunks
  await deleteChunksByFile(fileId)
  // 清理内存缓存
  import('./embeddingService').then(({ removeCachedVectors }) => removeCachedVectors(fileId))
  // 再删文件
  return new Promise(async (resolve, reject) => {
    // ... 原有逻辑
  })
}
```

**步骤 4: 验证构建**

运行：`npm run build`
预期：构建成功

**步骤 5: 提交**

```bash
git add src/services/ragPipeline.js src/services/storageService.js
git commit -m "feat: auto-index files on upload with chunk + embed pipeline"
```

---

## 任务 6: 替换 aiService 中的全量拼接为 RAG 检索

**文件：**
- 修改：`src/services/aiService.js` — 重写 `buildContextMessage()` 使用向量检索 + re-rank

**步骤 1: 重写 buildContextMessage**

将原来的全量拼接逻辑替换为 RAG 检索：

```javascript
async function buildContextMessage(userMessage, spacesContent, kbScope, getKBContent) {
  let contextParts = []

  if (spacesContent.articles && spacesContent.articles.trim()) {
    contextParts.push(`## 过往文章（用于参考行文风格）\n${spacesContent.articles}`)
  }

  // RAG 检索替代全量拼接
  if (kbScope && kbScope.type !== 'none') {
    try {
      const { embedTexts, searchSimilar, rerankChunks } = await import('./embeddingService')
      const { loadKbVectorsToCache } = await import('./ragPipeline')

      // 确保向量已加载到内存
      await loadKbVectorsToCache(kbScope.kbId)

      // 对用户消息做 embedding
      const [queryVector] = await embedTexts([userMessage])

      // 向量检索 top-6
      const topChunks = await searchSimilar(queryVector, 6)

      if (topChunks.length > 0) {
        // Re-rank 压到 3 条
        const reranked = await rerankChunks(userMessage, topChunks, 3)
        const ragContent = reranked.map((c, i) =>
          `### 参考片段 ${i + 1}（来源：${c.fileName}${c.heading ? ' > ' + c.heading : ''}）\n${c.text}`
        ).join('\n\n')
        contextParts.push(`## 知识库：${kbScope.label}（RAG 检索结果）\n${ragContent}`)
      } else {
        // 回退：如果没有向量数据，使用原来的全量方式
        const kbContent = await getKBContent(kbScope.kbId, kbScope.folderId)
        if (kbContent) contextParts.push(`## 知识库：${kbScope.label}\n${kbContent}`)
      }
    } catch (e) {
      console.warn('RAG 检索失败，回退到全量拼接:', e)
      const kbContent = await getKBContent(kbScope.kbId, kbScope.folderId)
      if (kbContent) contextParts.push(`## 知识库：${kbScope.label}\n${kbContent}`)
    }
  }

  if (contextParts.length > 0) {
    const context = contextParts.join('\n\n---\n\n')
    return `以下是创作者的背景素材，请结合这些内容完成创作任务：

${context}

---

${userMessage}`
  }

  return userMessage
}
```

关键设计：
- 如果 RAG 检索失败（无向量数据、API 错误），自动回退到原来的全量拼接，保证不中断用户体验
- `loadKbVectorsToCache` 有幂等性，重复调用不会重复加载

**步骤 2: 验证构建**

运行：`npm run build`
预期：构建成功

**步骤 3: 提交**

```bash
git add src/services/aiService.js
git commit -m "feat: replace full-text concat with RAG retrieval in AI context"
```

---

## 任务 7: UI 反馈 — 索引状态指示

**文件：**
- 修改：`src/components/KBFileList.jsx` — 上传后显示索引进度
- 修改：`src/styles/KBFileList.css`（如果存在）或对应样式文件

**步骤 1: 在 KBFileList 中添加索引状态**

在文件上传成功后，显示"正在索引..."状态，索引完成后显示 chunk 数量：

```jsx
const [indexingFiles, setIndexingFiles] = useState(new Set())

// 修改 handleFiles 中的上传逻辑
const handleFiles = async (fileList) => {
  for (const file of fileList) {
    try {
      const saved = await uploadKBFile(kbId, folderId, file)
      setIndexingFiles(prev => new Set([...prev, saved.id]))
      // 监听索引完成（uploadKBFile 内部异步触发，这里轮询或用事件）
      // 简单方案：3秒后移除索引状态
      setTimeout(() => {
        setIndexingFiles(prev => {
          const next = new Set(prev)
          next.delete(saved.id)
          return next
        })
      }, 5000)
    } catch (e) { /* ... */ }
  }
  loadFiles()
}
```

在文件列表渲染中，对正在索引的文件显示标记：

```jsx
{indexingFiles.has(file.id) && <span className="indexing-badge">索引中...</span>}
```

**步骤 2: 添加样式**

```css
.indexing-badge {
  font-size: 11px;
  color: #f59e0b;
  background: #fef3c7;
  padding: 1px 6px;
  border-radius: 4px;
  margin-left: 6px;
}
```

**步骤 3: 验证构建**

运行：`npm run build`
预期：构建成功

**步骤 4: 提交**

```bash
git add src/components/KBFileList.jsx src/styles/
git commit -m "feat: show indexing status badge on KB file upload"
```

---

## 任务 8: 端到端验证 + 回退测试

**步骤 1: 启动应用**

运行：`npm run electron:dev`

**步骤 2: 测试 RAG 流程**

1. 创建一个知识库，上传 2-3 个 md/txt 文件（总计 2000+ 字）
2. 等待索引完成（观察控制台无报错）
3. 在 AI 对话中选择该知识库，发送一个与文件内容相关的问题
4. 验证：
   - 回复内容基于知识库中的相关片段（而非全量内容）
   - 控制台可以看到 embedding 和 rerank 的 API 调用
   - prompt 中只包含 3 条参考片段，而非全部文件内容

**步骤 3: 测试回退**

1. 断开网络或使用无效 API key
2. 发送消息
3. 验证：回退到全量拼接模式，不中断用户体验

**步骤 4: 测试文件删除**

1. 删除一个已索引的文件
2. 验证：对应的 chunks 被清理（IndexedDB 和内存缓存）

**步骤 5: 提交最终状态**

```bash
git add -A
git commit -m "feat: Phase 1 RAG complete - vector search replaces full-text concat"
```

---

## 依赖关系

```
任务 1 (IPC) ──┐
               ├──→ 任务 3 (Embedding 服务) ──→ 任务 5 (Pipeline) ──→ 任务 6 (AI 集成) ──→ 任务 8 (验证)
任务 2 (Chunker)┘                                     │
                                                      └──→ 任务 7 (UI)
任务 4 (IndexedDB) ──→ 任务 5 (Pipeline)
```

## 风险与回退

| 风险 | 缓解 |
|------|------|
| Gemini embedding API 不可用 | `buildContextMessage` 自动回退到全量拼接 |
| Re-rank 返回格式异常 | `rerankChunks` catch 后回退到向量排序 top-3 |
| IndexedDB 升级失败（v2→v3） | `onupgradeneeded` 只在 `oldVersion < 3` 时创建新 store，不影响已有数据 |
| 大文件 embedding 超时 | 分批 20 条调用，单批失败不影响其他批次 |
